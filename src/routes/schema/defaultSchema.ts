
export const defaultMessage = {
    type: "object",
    properties: {
        message: { type: "string" }
    }
}

export const defaultError = {
    type: "object",
    properties: {
        message: { type: "string" },
        error: { type: "string" }
    }
}